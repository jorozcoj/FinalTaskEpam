# FinalTaskEpam

### Description
This project automates login tests for [SauceDemo](https://www.saucedemo.com/) using **WebdriverIO with mocha.

### Test Cases

**UC-1**: Login form with empty credentials (check "Username is required")
**UC-2**: Login form with cleared password (check "Password is required")
**UC-3**: Login form with valid credentials (verify dashboard title "Swag Labs")

## 🧰 Installation

1. **Clone the repository:**

   ```bash
   git clone https://github.com/yourusername/qa-automation-saucedemo.git
   cd qa-automation-saucedemo